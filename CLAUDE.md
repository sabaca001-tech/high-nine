# CLAUDE.md — 開発ルール

このファイルは Claude Code がこのリポジトリで作業する際の指示書です。
仕様の全体像は `README.md`、詳細仕様は `docs/` を参照してください。

---

## 0. 最初に読むもの

1. `README.md` … ゲーム仕様とディレクトリ構成
2. `src/core/types/` … 型定義。**実装前に必ず型を確認する**
3. `docs/balance.md` … 数値バランスの根拠

---

## 1. 絶対に守るルール

### 1.1 レイヤー分離（最重要）
```
src/core/  … ゲームロジック。React・DOM・localStorage を import してはいけない
src/state/ … core と UI の橋渡し（Zustand）
src/ui/    … 表示のみ。ここでゲームルールを計算してはいけない
src/save/  … 永続化。core の型を参照するのは可
```
- `src/core` の中で `import React` / `document` / `window` / `localStorage` を書いたら **設計違反**。
- UIコンポーネントの中で「能力値がいくつ上がるか」を計算しない。必ず `core` の関数を呼ぶ。

### 1.2 乱数は必ずシード付きPRNGを使う
```ts
// ❌ 禁止
const n = Math.random();

// ✅ 正しい
import { createRng } from '@/core/rng/random';

const rng = createRng(state.rngState);   // GameState から取り出す
const n = rng.int(1, 5);
const card = rng.pick(cards);
return { ...state, rngState: rng.state }; // 最後に必ず書き戻す
```
理由：セーブ＆リロードで結果が変わるバグを防ぎ、テストを再現可能にするため。

- `GameState` に持たせるのは **`rngState`（数値ひとつ）** だけ。`Rng` カーソル自体を state に入れない。
- カーソルは1コマンドの処理中だけ使う使い捨て。**最後に `rng.state` を書き戻すのを忘れない**（忘れると毎回同じ乱数が出る）。
- 例外は `createSeed()` のみ。ゲーム開始時の初期シード生成だけは `Math.random()` を使ってよい。

### 1.3 core の関数は純粋関数にする
- 引数を破壊的に変更しない（`player.meet += 3` ではなく新しいオブジェクトを返す）。
- 副作用（保存・ログ出力・時刻取得）を core に入れない。
- 戻り値は「新しい state」と「発生したイベントの配列」の形にする。

```ts
type EngineResult = {
  state: GameState;
  events: GameEvent[];   // UIはこれを見て演出する
};
```

### 1.4 マジックナンバーを直書きしない
バランス調整値は必ず定数ファイル（`cardDefs.ts` / `boardDefs.ts` / `balance.ts`）にまとめる。
「成長量 +3」のような数字をロジックの中に埋め込まない。

---

## 2. コーディング規約

| 項目 | ルール |
|---|---|
| TypeScript | `strict: true`。`any` 禁止。やむを得ない場合は `unknown` ＋ 型ガード |
| 命名 | 型・コンポーネント＝PascalCase / 関数・変数＝camelCase / 定数＝UPPER_SNAKE_CASE |
| ファイル名 | ロジック＝camelCase.ts / コンポーネント＝PascalCase.tsx |
| import | 絶対パス `@/core/...`（`tsconfig` の paths で設定） |
| コメント | **日本語で書く。** ゲーム的な意図（なぜその数値か）を残す |
| 1ファイル | 目安200行。超えたら分割を検討 |
| React | 関数コンポーネントのみ。クラス禁止 |
| CSS | CSS Modules または `tokens.css` の変数を使用。インラインの色指定は禁止 |

### 型の書き方
- 状態を表す型は `type`、拡張前提のものは `interface`。
- 文字列の種別は union 型で定義する（`type CellKind = 'practice' | 'good' | 'bad' | ...`）。enum は使わない。

---

## 3. スマホUIの必須要件

- **縦画面のみ**を想定。横画面レイアウトは作らない。
- タップ領域は **最小 44×44px**。
- 主要な操作ボタンは **画面下部1/3**（親指の届く範囲）に置く。
- `env(safe-area-inset-*)` でノッチ・ホームバーを避ける。
- 想定基準幅は **375px**。`vw` や `clamp()` で可変にする。
- `100vh` は使わない（モバイルSafariでズレる）→ `100dvh` を使う。
- スクロールが必要なUIは、下部ナビと重ならないよう `padding-bottom` を確保する。

---

## 3.5 ビジュアル方針

**画像ファイルを使わない。絵はすべて SVG と CSS で描く。**
理由：外部通信なしで動き、季節・チームカラーの差し替えが色変数の変更だけで済み、
アセット管理とバンドルサイズの問題が発生しないため。

- 色はすべて `src/ui/theme/tokens.css` の CSS 変数に置く。
  `.tsx` の中に `#rrggbb` を書かない（SVGの `fill` も `var(--xxx)` を使う）。
- 選手の絵は `PlayerSprite`（全身・ポーズ付き）と `PlayerFace`（一覧用の顔だけ）を使い回す。
- グラウンドの情景は `FieldScene`。季節は `data-season` 属性で色変数ごと切り替える。
- アニメーションは CSS の `transform` / `opacity` のみで行う（レイアウトを動かさない）。
- **`@media (prefers-reduced-motion: reduce)` で必ずアニメーションを止める。**
  動きが苦手な人への配慮であり、電池の節約にもなる。
- 装飾は情報を隠さない。文字がグラウンドと重なる場合は暗いスクリムを敷く。

### 部費の収支を測るとき
月ごとの残高の増減で測ってはいけない。収入と支出が同じ月に相殺されて、
**支出がほぼ0に見える**（実際にそう見えて「遠征費が効いていない」と誤判断した）。
`seasonBalance.test.ts` のようにログから項目ごとに拾うこと。
ログは `LOG_LIMIT` 件で切り捨てられるので、年ごとではなく毎月走査する。

### 恒久強化には「下がる」仕組みを入れる
グラウンドや練習器具のように上げっぱなしにできる強化は、部費が貯まった時点で
最大に固定され、以降の判断が消える。**毎月一定の確率で下がる／壊れる**ようにして、
維持し続けること自体を選択にする。

### 手札に出るカードを増減させるとき
練習器具のように「持っていないと出ないカード」を足したら、
**失ったときに手札から取り除く**ことまでセットで実装する
（`replaceBrokenCards`）。使えない練習が手札に残ると、
選んでも何も起きないカードになる。

### 部費とアイテム
- アイテムは **買った瞬間に効果が出る**。在庫・持ち物の管理は作らない。
  「買う→使う」の2手間はスマホでは煩雑なだけで、判断の質が変わらないため。
- 価格と効果は `src/core/shop/itemDefs.ts`、収入は `funds.ts`、
  遠征費は `travel.ts`、毎月の維持費は `upkeep.ts` に集約する。
- **恒久的な強化を足すときは、維持費を持たせるか検討する。**
  払い切りだと「部費が貯まったら必ず買う」で判断が消える。
  維持費を付けると「買ったあと払い続けられるか」を考える必要が出る。
  付けた場合は購入ボタンに増える維持費を必ず表示する。
- 部費を消費するコマンドは、**必ず残高を確認してから**適用する
  （足りなければ state をそのまま返す）。

### 進行を止める要素を足すとき
怪我のように**選手が一時的に使えなくなる**仕組みを足したら、
`autoLineup` / `repairLineup` が必ずその選手を外すことを確認する。
スタメンに残ったままだと、試合に出られない選手が出場してしまう。

### プレイヤーが避けられない要素
日付固定イベントのように**プレイヤーが回避できない**ものに、大きなマイナスを置かない。
理不尽になるだけで判断の余地が生まれないため。
避けられないマイナスは軽く、選択の結果として受けるマイナスは重く、が原則。

### ハッシュから見た目を決めるとき
`PlayerPortrait` のように id のハッシュでパーツを選ぶ場合、
**符号なしシフト（`>>>`）を使う**。`>>` だと 2^31 以上のハッシュで負の添字になり、
`undefined` を引いて画面が真っ白になる（実際に起きた）。
型チェックでは捕まらないので、**必ずブラウザで一覧表示まで確認する**。

### グラフを描くとき
`AbilityChart` の方針に合わせる。グラフ用のライブラリは入れない（SVGで足りる）。

- **項目が多いときは1項目1グラフにして縦に並べる**（small multiples）。
  1枚に重ねると線が混ざって読めない。
- **縦軸は全項目で共通に固定する**（能力なら1〜100）。項目ごとに自動スケールすると
  「+1しか伸びていない項目」が「+30伸びた項目」と同じ傾きに見えて誤読される。
- 系列が1本のときは凡例を置かない。見出しが系列名を兼ねる。
- 点ごとに数字を書かない。スマホでは潰れる。**触れた位置の値を出す**方式にする。
- 色は系列ではなく項目に紐づける。並び順で色を変えない。

### 見た目を state に持たせるとき
ユニフォームの色を**学校名のハッシュ**から導いていたので、
学校名を変えると色まで変わり、そもそも選べなかった。
プレイヤーに選ばせたい見た目は、必ず id を state に持つ。

**core が色コードを知ってはいけない。** core は id だけを持ち
（`core/team/uniforms.ts`）、CSS 変数への対応づけは UI 側
（`ui/theme/playerColors.ts`）に置く。そうしないとテーマを差し替えられない。

### 小さい整数に丸めると個人差が消える
成長の補正（やる気・学年・体力・性格）は前からあったのに、
1回の伸びが1〜2という小さな値になったせいで**丸めた時点で差が消え、
画面には全員「+1」しか出てこなかった**。
補正を足すときは、それが**丸めを跨ぐだけの幅を持っているか**を確認すること。
足りなければ、幅の大きい係数（`growthAptitude` のような0.6〜1.6）を別に用意する。

### 集計して見せると「全員同じ」に見える
練習結果を能力ごとに集計して「ミート +1.0 ×6人」と出していた。
数字は正しいが、**誰が伸びたのか分からない**うえ、平均を出すと
全員が同じだけ伸びたように見える。育成ゲームで知りたいのは
「今の練習で誰が伸びたか」なので、名前で並べる。

### 上限を作ったら下限も作る
評判は「上に行くほど上がりにくい」だけを入れていた。
そこへ**負けたら下がる**を足したら、勝てないチームの評判が一桁まで落ちて、
新入生も部費も枯れて立て直せなくなった（実測で60年後に7）。
片側だけ鈍らせると、逆側に張り付く。
`reputationGainAt` は上下どちらも鈍らせている。

### 別の物差しに移すときは、その物差しの定数も全部直す
プロ入りで能力を半分にしたとき、戦力外の閾値（45）や出場率の式を
高校基準のまま残していたら、**入団した年に全員が戦力外**になるところだった。
スケールを変えたら、そのスケールを読む定数を洗い出して同時に直すこと
（`career.ts` の `RELEASE_THRESHOLD` / `OVERSEAS_THRESHOLD` / `playRate`）。

### 相対評価の仕組みを足すとき
U18代表のように「他校と比べて」で決まる仕組みは、**比較対象を state に持つ**。
その場で作った相手と比べても、翌年には別人になっていて基準が動かない。
`rivals`（ライバル校）が基準を持ち続ける役割を担っている。

さらに、**「いちばん強い1人」と比べてはいけない。**
その1人が卒業した年だけ基準が急に下がる。
枠が18人あるなら**18番目の選手**を基準にする（`upperStarRatingAtRank`）。
比較の母集団も、県内ではなく**全国**にする。県内にすると
「参加校の多い県を選ぶと代表に選ばれにくい」という筋の通らない差がつく。

### 「他校」を足すとき
選手データを他校ぶんまで丸ごと持つとセーブが膨らむ。
持つのは「学校の戦力」と「注目選手（2人）」だけにして、
試合に出てくる9人は戦力値から使い捨てで作る。

戦力は**必ず地力へ引き戻す**（`REGRESSION`）。引き戻さないと
強豪が際限なく強くなるか、弱小が消えていく。

### `Math.round` は -0 を返す
`Math.round(-0.2)` は **-0**。JSON にすると `0` になるので、
セーブして読み直すと値が変わり「JSONに変換できる」テストが落ちる（実際に落ちた）。
負になりうる値を丸めるときは 0 に潰すこと。

### 「外す」操作に自動補充を足さない
ベンチ入りから外す仕組みで、定員を自動補充していたため
**落とした選手がいちばん強い控えとしてその場で戻ってきて**、入れ替えができなかった。
プレイヤーの操作（`trimSquad`）は減ったままにし、
補充するのは部員が入れ替わったとき（`repairSquad`）だけにする。

### 画面下部の操作を押し出さない
可変長のパネル（成長結果など）は**必ず高さの上限を置く**。
上限が無いと下にある手札やボタンを画面外へ押し出す（実際に押し出された）。
`max-height` ＋ `overflow-y: auto` で中身をスクロールさせる。

### ドラッグ操作を作るとき
**HTML5 の drag&drop（draggable / dragstart）は使わない。** タッチ端末で動かない。
`pointerdown` / `pointermove` / `pointerup` で自前に組み、
掴んでいる要素には `touch-action: none` を付ける（付けないとスクロールに取られる）。
落とし先は `document.elementFromPoint()` と `data-*` 属性で拾う。

**`touch-action: none` は「つまみ」だけに付ける。** 行全体に付けると、
その行の上では**ページがスクロールできなくなる**（スタメン画面が見出し付近でしか
スクロールできず、実際に操作不能になった）。行の左端に専用のつまみ（`⠿`）を置き、
そこにだけ `touch-action: none` を置く。行の他の場所は指を滑らせればスクロールする。

### 見た目でチームと個人を区別する
帽子とユニフォームは**チームで共通**にする（`teamColors(schoolName)`）。
同じ部の選手なのに一人ひとり色が違うのは不自然。
選手ごとの区別は**ポジションの系統で色分けしたネームプレート**で付ける。

### カード全体をタップできるようにするとき
カード内にボタンを置くなら、**カード自体を `<button>` にしない**。
`<button>` の入れ子は不正なHTMLで、React が実行時エラーを出す（実際に出た）。
型チェックでは捕まらないので、**必ずブラウザのコンソールまで確認する**。

### アセットを増やさない
画像と同じ理由で、**音声ファイルも持たない**。効果音は `src/ui/sound/sound.ts` で
Web Audio API から合成する。外部通信なしで鳴り、オフラインでも動く。

- `AudioContext` は**最初のユーザー操作まで作らない**（ブラウザの自動再生制限）。
- 効果音のON/OFFは端末の設定なので localStorage に保存し、**セーブデータには入れない**。
- 描画ライブラリ（Pixi.js など）は入れない。SVGで足りている。

### PWA / Service Worker
- `public/sw.js` は**ビルドのファイル名ハッシュに依存させない**。
  事前にファイル一覧を焼き込むと、ビルドのたびに壊れる。
- 開発中は登録しない（`import.meta.env.PROD` で分岐）。HMRと衝突する。
- キャッシュ名 `CACHE` を変えると古いキャッシュが破棄される。
  配信物の構成を大きく変えたときは上げること。

### 権利について
本家タイトルのアセット・キャラクターデザイン・ロゴを模倣しない。
参考にしてよいのは「画面構成」（情景を常時表示する、カードを絵で見せる等）までとする。

## 4. テスト

- `src/core` に書いたロジックは **ユニットテストを必ず添える**（`*.test.ts` を同じ階層に置く）。
- UIコンポーネントのテストは MVP 段階では不要。
- テスト対象の優先度：成長計算 > マス解決 > カード抽選 > 打席シミュレーション。
- 乱数を使う関数は、固定シードを渡して結果を検証する。

```bash
npm run test
npm run typecheck   # コミット前に必ず通す
```

---

## 5. 作業の進め方

### 実装を始める前に
1. 変更対象がどのレイヤー（core / state / ui / save）か明示する。
2. 新しい型が必要なら **先に `src/core/types/` を更新** してから実装に入る。
3. 3ファイル以上に触れる変更は、先に手順を提示して確認を取る。

### 実装するとき
- **一度に1機能だけ。** 依頼されていないリファクタリングや機能追加をしない。
- 既存のコードスタイル・命名に合わせる。
- 実装後は `npm run typecheck` と `npm run test` を実行し、結果を報告する。
- 動かないもの・未検証のものを「完成しました」と報告しない。

### やらないこと
- 依頼なしでライブラリを追加しない（追加が必要なら理由と代替案を提示して確認する）。
- 依頼なしで `README.md` / `CLAUDE.md` を書き換えない。
- 実在の球団名・選手名・大会名・楽曲・ロゴを使わない（**権利侵害の回避**）。
- 大量のファイルを一度に生成しない。

---

## 5.5 試合を触るときの注意

**シミュレーション（結果を決める）と再生（表示する）を絶対に混ぜない。**

さらに、**試合はマスに止まった時点ではシミュレートしない。**
`lineupCheck` フェーズでスタメンを確認させ、`startMatch` で初めて
試合を組み立てる。先に走らせると、確認画面で編成を変えても結果が変わらない。

**試合は半回ずつ進む。** `stepHalfInning()` を呼んだ回数だけ進み、
回の切れ目で `MatchState` が state に残る。ここに選手交代を挟む。

- `MatchState` は **JSON にそのまま変換できる形しか持たない**（Map / Set 禁止）。
  中断してセーブされるため。`Player.history` は試合に使わないので落とす。
- 交代を挟まなければ乱数の並びは変わらないので、
  **どの速度で見ても結果は同じ**という保証はそのまま生きている。
- テストで試合を最後まで進めるときは `autoPlay.ts` の `runMatch()` を使う。
  `startMatch` だけでは1球も投げていない状態で止まる。

```
startMatchState() → MatchState
      ↓ stepHalfInning() を半回ずつ          ← core、純粋関数
finalizeMatch()   → MatchResult（全打席の記録）
                      ↓
        UI が speed に応じて再生するだけ      ← ui、結果に影響しない
```

- `src/core/match/` に演出・タイミング・setTimeout を持ち込まない。
- 観戦速度を変えても結果が変わってはいけない。**速度は表示だけの設定**。
- 試合結果のチームへの反映（信頼度・体力）は `finishMatch` でのみ行う。
  観戦中に反映すると、途中離脱で二重適用される。
- 試合中は他画面へ移動させない（`App.tsx` で `phase === 'match'` を最優先で分岐）。

## 5.55 盤面は1マス1日・1年365マス

月ごとのゴールは無い。4月1日から3月31日まで1本の道が続く。

- **1マス＝1日**（`DAYS_PER_CELL`）。1年は `CELLS_IN_YEAR = 365` マス。
  日とマスの換算は `src/core/calendar/days.ts` の `dayOfCell()` / `cellOfDay()` だけを使う。
  いまは恒等写像だが、**マス番号と日を直接同一視するコードを書かない**
  （3日刻みにしていた時期があり、また変えるかもしれない）。
- **手数が変わったら1手あたりの効果を割り戻す。**
  カードの数字は1〜5のまま刻みだけ1日に戻したので、1年の手数が約49手→約146手になった。
  そのぶんを `PRACTICE_GROWTH_SCALE`（練習1回）と `CARD_COST_SCALE`（体力・信頼度）で吸収している。
  カード定義の `amount` は「1回の練習でこれくらい」という読みやすい値のまま残す。
- **マスの重みも組み直す。** 手数が3倍になると、練習試合も特訓も怪我も年3倍起きる。
  `CELL_WEIGHTS` は「1年に何回あってほしいか」から逆算し、増えたぶんは
  練習マスと空きマスに寄せてある。
- 月をまたぐ処理（部費・維持費・体力回復・急成長・固定イベント・能力の記録）は
  `applyOneMonth()` にまとめ、`selectCard` が **またいだ月ぶん繰り返し呼ぶ**。
  カード1枚で2ヶ月ぶんまたぐことがあるので、1回だけ呼ぶ実装にしてはいけない。
- **4月（年度初め）も同じ月次処理を通す。** 通さないと4月の行事（入学式）だけ起きない。
  `advanceYear` の中で明示的に呼んでいる。
- 大会・合宿のマスは `forcedStopBetween()` で**飛び越えを禁止**する。
- **大会は1回戦ずつ別のマスに置く。** `placeTournamentCells()` が現在地の先へ
  `ROUND_GAP` マス間隔で回戦ぶんのマスを並べる。1つ勝つたびに `cardSelect` へ戻り、
  次の回戦のマスまでは普通に練習できる。負けた／優勝したら
  `clearTournamentCells()` で残りのマスを消す。**消さないと同じ大会が再開する。**
- **カードの数字は「進むマス数（1〜5）」。ここを変えると1年の手数が変わり、
  練習回数＝成長速度が直接動く。** 必ず `seasonBalance.test.ts` を回して確認する。

### 小さい値を四捨五入しない
1手あたりの体力消耗が2〜3という小さな値になったとき、`Math.round` で丸めたら
**性格やマネージャーの2割の差が消えた**（「やんちゃは消耗が激しい」が成立しなくなった）。
1未満の差を持つ値は、成長計算と同じ**確率で切り上げる**丸めにする。
そのぶんテストも1回では判定できないので、平均を取って比べること。

## 5.6 新しいフェーズを足すときの注意

`GameState.phase` に値を足したら、次の3つを必ずセットで用意する。

1. `App.tsx` … そのフェーズ専用の画面へ強制的に飛ばす（他画面へ行かせない）
2. **`src/core/autoPlay.ts` の `playUntilMonthEnd()` に分岐を足す**
3. そのフェーズから抜けるコマンド（`finishMatch` / `finishSeason` / `chooseCampPlan` など）

`match` / `newSeason` / `tournament` / `camp` を足したとき、**4回とも**進行ループが止まった。
当初は診断・テストそれぞれに同じ分岐を書いていたため毎回あちこちが壊れたので、
**進行ループは `autoPlay.ts` に一本化した**。ここだけ直せば全部追従する。

さらに `tournament` では、月を進める前に開催判定をしていたため、
大会終了後に同じ月で無限に再開催されるループも起きた。
**フェーズから抜ける経路が「元の判定に戻らない」ことを必ず確認する。**

フェーズを足したら、単体テストだけでなく**必ず診断スクリプトを走らせる**：
```bash
npx vitest run src/core/seasonBalance.test.ts --disable-console-intercept
```

## 6. セーブデータの扱い

- `GameState` は **そのまま JSON にシリアライズできる形** に保つ（Map / Set / class / 関数を入れない）。
- `GameState` に `version: number` を必ず持たせる。
- 型を破壊的に変更したら `src/save/migrate.ts` に移行処理を追加する。
- 保存キーは **1枠目が `hs-baseball-sim:save:v1` 固定**（キーは変えず、中の `version` で管理する）。
  2枠目以降は `:2` `:3` を足す。**1枠目のキーを変えてはいけない**（既存のセーブが読めなくなる）。
- 現在の `SAVE_VERSION` は 26。v1→v26 の移行が `migrate.ts` にある。
  **盤面の刻み（`DAYS_PER_CELL`）を変えたら移行が必須。** `boardPosition` の意味が
  変わるので、`cellOfDay()` で読み替えて盤面を作り直す（v22→v23 がその例）。
- `Player.history`（能力の推移）のように**毎月増える配列**を足すときは、必ず上限を決めて
  `slice(-LIMIT)` で切る。セーブが際限なく膨らんで localStorage の容量を超える。

---

## 7. コミットメッセージ

```
feat: 練習カードの抽選ロジックを追加
fix: 能力値が上限100を超えるバグを修正
refactor: マス解決処理を resolveCell に切り出し
docs: 成長曲線のバランスメモを追記
test: growth.ts のユニットテストを追加
chore: Vitest を導入
```
- 本文は日本語。1コミット1目的。

---

## 8. 応答スタイル

- 回答・説明・コード内コメントは**日本語**。
- 変更内容は「どのファイルを・なぜ変えたか」を簡潔に述べる。長い前置きは不要。
- 仕様が曖昧な場合は、勝手に決めずに確認する。ただし細かな実装判断（変数名など）は自分で決めてよい。
