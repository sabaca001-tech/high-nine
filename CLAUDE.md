# CLAUDE.md — 開発ルール

このファイルは Claude Code がこのリポジトリで作業する際の指示書です。
仕様の全体像は `README.md`、詳細仕様は `docs/` を参照してください。

---

## 0. 最初に読むもの

1. `README.md` … ゲーム仕様とディレクトリ構成
2. `src/core/types/` … 型定義。**実装前に必ず型を確認する**
3. `docs/balance.md` … 数値バランスの根拠

---

## 1. 絶対に守るルール

### 1.1 レイヤー分離（最重要）
```
src/core/  … ゲームロジック。React・DOM・localStorage を import してはいけない
src/state/ … core と UI の橋渡し（Zustand）
src/ui/    … 表示のみ。ここでゲームルールを計算してはいけない
src/save/  … 永続化。core の型を参照するのは可
```
- `src/core` の中で `import React` / `document` / `window` / `localStorage` を書いたら **設計違反**。
- UIコンポーネントの中で「能力値がいくつ上がるか」を計算しない。必ず `core` の関数を呼ぶ。

### 1.2 乱数は必ずシード付きPRNGを使う
```ts
// ❌ 禁止
const n = Math.random();

// ✅ 正しい
import { createRng } from '@/core/rng/random';

const rng = createRng(state.rngState);   // GameState から取り出す
const n = rng.int(1, 5);
const card = rng.pick(cards);
return { ...state, rngState: rng.state }; // 最後に必ず書き戻す
```
理由：セーブ＆リロードで結果が変わるバグを防ぎ、テストを再現可能にするため。

- `GameState` に持たせるのは **`rngState`（数値ひとつ）** だけ。`Rng` カーソル自体を state に入れない。
- カーソルは1コマンドの処理中だけ使う使い捨て。**最後に `rng.state` を書き戻すのを忘れない**（忘れると毎回同じ乱数が出る）。
- 例外は `createSeed()` のみ。ゲーム開始時の初期シード生成だけは `Math.random()` を使ってよい。

### 1.3 core の関数は純粋関数にする
- 引数を破壊的に変更しない（`player.meet += 3` ではなく新しいオブジェクトを返す）。
- 副作用（保存・ログ出力・時刻取得）を core に入れない。
- 戻り値は「新しい state」と「発生したイベントの配列」の形にする。

```ts
type EngineResult = {
  state: GameState;
  events: GameEvent[];   // UIはこれを見て演出する
};
```

### 1.4 マジックナンバーを直書きしない
バランス調整値は必ず定数ファイル（`cardDefs.ts` / `boardDefs.ts` / `balance.ts`）にまとめる。
「成長量 +3」のような数字をロジックの中に埋め込まない。

---

## 2. コーディング規約

| 項目 | ルール |
|---|---|
| TypeScript | `strict: true`。`any` 禁止。やむを得ない場合は `unknown` ＋ 型ガード |
| 命名 | 型・コンポーネント＝PascalCase / 関数・変数＝camelCase / 定数＝UPPER_SNAKE_CASE |
| ファイル名 | ロジック＝camelCase.ts / コンポーネント＝PascalCase.tsx |
| import | 絶対パス `@/core/...`（`tsconfig` の paths で設定） |
| コメント | **日本語で書く。** ゲーム的な意図（なぜその数値か）を残す |
| 1ファイル | 目安200行。超えたら分割を検討 |
| React | 関数コンポーネントのみ。クラス禁止 |
| CSS | CSS Modules または `tokens.css` の変数を使用。インラインの色指定は禁止 |

### 型の書き方
- 状態を表す型は `type`、拡張前提のものは `interface`。
- 文字列の種別は union 型で定義する（`type CellKind = 'practice' | 'good' | 'bad' | ...`）。enum は使わない。

---

## 3. スマホUIの必須要件

- **縦画面のみ**を想定。横画面レイアウトは作らない。
- タップ領域は **最小 44×44px**。
- 主要な操作ボタンは **画面下部1/3**（親指の届く範囲）に置く。
- `env(safe-area-inset-*)` でノッチ・ホームバーを避ける。
- 想定基準幅は **375px**。`vw` や `clamp()` で可変にする。
- `100vh` は使わない（モバイルSafariでズレる）→ `100dvh` を使う。
- スクロールが必要なUIは、下部ナビと重ならないよう `padding-bottom` を確保する。

---

## 3.5 ビジュアル方針

**画像ファイルを使わない。絵はすべて SVG と CSS で描く。**
理由：外部通信なしで動き、季節・チームカラーの差し替えが色変数の変更だけで済み、
アセット管理とバンドルサイズの問題が発生しないため。

- 色はすべて `src/ui/theme/tokens.css` の CSS 変数に置く。
  `.tsx` の中に `#rrggbb` を書かない（SVGの `fill` も `var(--xxx)` を使う）。
- 選手の絵は `PlayerSprite`（全身・ポーズ付き）と `PlayerFace`（一覧用の顔だけ）を使い回す。
- グラウンドの情景は `FieldScene`。季節は `data-season` 属性で色変数ごと切り替える。
- アニメーションは CSS の `transform` / `opacity` のみで行う（レイアウトを動かさない）。
- **`@media (prefers-reduced-motion: reduce)` で必ずアニメーションを止める。**
  動きが苦手な人への配慮であり、電池の節約にもなる。
- 装飾は情報を隠さない。文字がグラウンドと重なる場合は暗いスクリムを敷く。

### 部費の収支を測るとき
月ごとの残高の増減で測ってはいけない。収入と支出が同じ月に相殺されて、
**支出がほぼ0に見える**（実際にそう見えて「遠征費が効いていない」と誤判断した）。
`seasonBalance.test.ts` のようにログから項目ごとに拾うこと。
ログは `LOG_LIMIT` 件で切り捨てられるので、年ごとではなく毎月走査する。

### 恒久強化には「下がる」仕組みを入れる
グラウンドや練習器具のように上げっぱなしにできる強化は、部費が貯まった時点で
最大に固定され、以降の判断が消える。**毎月一定の確率で下がる／壊れる**ようにして、
維持し続けること自体を選択にする。

### 手札に出るカードを増減させるとき
練習器具のように「持っていないと出ないカード」を足したら、
**失ったときに手札から取り除く**ことまでセットで実装する
（`replaceBrokenCards`）。使えない練習が手札に残ると、
選んでも何も起きないカードになる。

### 部費とアイテム
- アイテムは **買った瞬間に効果が出る**。在庫・持ち物の管理は作らない。
  「買う→使う」の2手間はスマホでは煩雑なだけで、判断の質が変わらないため。
- 価格と効果は `src/core/shop/itemDefs.ts`、収入は `funds.ts`、
  遠征費は `travel.ts`、毎月の維持費は `upkeep.ts` に集約する。
- **恒久的な強化を足すときは、維持費を持たせるか検討する。**
  払い切りだと「部費が貯まったら必ず買う」で判断が消える。
  維持費を付けると「買ったあと払い続けられるか」を考える必要が出る。
  付けた場合は購入ボタンに増える維持費を必ず表示する。
- 部費を消費するコマンドは、**必ず残高を確認してから**適用する
  （足りなければ state をそのまま返す）。

### 進行を止める要素を足すとき
怪我のように**選手が一時的に使えなくなる**仕組みを足したら、
`autoLineup` / `repairLineup` が必ずその選手を外すことを確認する。
スタメンに残ったままだと、試合に出られない選手が出場してしまう。

### プレイヤーが避けられない要素
日付固定イベントのように**プレイヤーが回避できない**ものに、大きなマイナスを置かない。
理不尽になるだけで判断の余地が生まれないため。
避けられないマイナスは軽く、選択の結果として受けるマイナスは重く、が原則。

### ハッシュから見た目を決めるとき
`PlayerPortrait` のように id のハッシュでパーツを選ぶ場合、
**符号なしシフト（`>>>`）を使う**。`>>` だと 2^31 以上のハッシュで負の添字になり、
`undefined` を引いて画面が真っ白になる（実際に起きた）。
型チェックでは捕まらないので、**必ずブラウザで一覧表示まで確認する**。

### グラフを描くとき
`AbilityChart` の方針に合わせる。グラフ用のライブラリは入れない（SVGで足りる）。

- **項目が多いときは1項目1グラフにして縦に並べる**（small multiples）。
  1枚に重ねると線が混ざって読めない。
- **縦軸は全項目で共通に固定する**（能力なら1〜100）。項目ごとに自動スケールすると
  「+1しか伸びていない項目」が「+30伸びた項目」と同じ傾きに見えて誤読される。
- 系列が1本のときは凡例を置かない。見出しが系列名を兼ねる。
- 点ごとに数字を書かない。スマホでは潰れる。**触れた位置の値を出す**方式にする。
- 色は系列ではなく項目に紐づける。並び順で色を変えない。

### 見た目を state に持たせるとき
ユニフォームの色を**学校名のハッシュ**から導いていたので、
学校名を変えると色まで変わり、そもそも選べなかった。
プレイヤーに選ばせたい見た目は、必ず id を state に持つ。

**core が色コードを知ってはいけない。** core は id だけを持ち
（`core/team/uniforms.ts`）、CSS 変数への対応づけは UI 側
（`ui/theme/playerColors.ts`）に置く。そうしないとテーマを差し替えられない。

### 尺度の変換は1か所に置く
球速（km/h）を1〜100に直す式を、判定・総合・レーダーの3か所に別々に書いていた。
上限が160/160/160、下限が115/120/110とバラバラで、
**同じ球速が画面と判定で違う意味になっていた**。
実数値を他の能力と同じ尺度に直すような変換は、型の定義側に1つだけ置く
（`types/player.ts` の `velocityScore`）。

### 派生する値は導出する
投手の肩力のように「他の能力から決まる」ものは、独立して持たせない。
`armFromVelocity` で球速から導き、**球速が動いたら一緒に動かす**
（`raiseAbility` の中）。別々に育つと、
球速150km/hなのに肩力Fという選手が生まれる。

### 説明の文字で一覧を押し出さない
スタメン画面で「タップで能力表示 → …」の帯と「おまかせ」の行が
**90px（選手2人ぶん）を文字だけで消費**していた。
一覧が主役の画面では、説明は見出しに同居させるか消す。
状態の案内が要る場合は、**一覧の高さを動かさない場所**（右の枠など）に出すこと。
一覧の上に出し入れすると行がずれて、タップが隣の行に当たる。

### ドラッグだけの操作は「届かない組み合わせ」を作る
一覧が長いと、掴む側と落とす側が**同時に画面に入らない**。
スタメンの上のほうとベンチ入りの下のほうを入れ替えられなかった。
並べ替えを作るときは、**タップで選んで、もう1回タップで確定**する経路も用意する。
選択中は色で強調し、次のタップで何が起きるかを言葉でも出すこと。

### まとめて配る報酬は手応えから切り離される
大会の成長を終了時にまとめて配っていたので、
準決勝で伸びた選手が決勝で活きず、勝ち上がっている実感が最後の画面まで来なかった。
**得たものはその場で返す。**

### 順番に取ると、先に取った枠が得をする
打順を1番から順に埋めていたら、上位打線が先に良い打者を抜いてしまい、
**「チームで最も優秀な打者」を置くはずの3番が1番より弱い**という並びになった。
枠に優先順位があるなら、**強い制約のある枠から先に埋める**こと
（9番＝打力最下位 → 中軸 → 1・2番 → 残り）。

### 平均を取ると個人差が消える
能力ごとに独立して振った乱数は、**その平均（総合）ではほぼ打ち消し合う**。
±14で振っても総合の振れは±3程度にしかならず、
全能力をまとめて動かす値（学年のベース）だけが総合に残っていた。

「この選手は格が違う」を作りたいなら、**全能力をまとめて動かす軸**を別に持つこと
（`TALENT_SPREAD`）。能力ごとの乱数（`ABILITY_SPREAD`）は
「打撃は良いが守備は苦手」という凸凹を作るためのもので、役割が違う。

### 小さい整数に丸めると個人差が消える
成長の補正（やる気・学年・体力・性格）は前からあったのに、
1回の伸びが1〜2という小さな値になったせいで**丸めた時点で差が消え、
画面には全員「+1」しか出てこなかった**。
補正を足すときは、それが**丸めを跨ぐだけの幅を持っているか**を確認すること。
足りなければ、幅の大きい係数（`growthAptitude` のような0.6〜1.6）を別に用意する。

### 集計して見せると「全員同じ」に見える
練習結果を能力ごとに集計して「ミート +1.0 ×6人」と出していた。
数字は正しいが、**誰が伸びたのか分からない**うえ、平均を出すと
全員が同じだけ伸びたように見える。育成ゲームで知りたいのは
「今の練習で誰が伸びたか」なので、名前で並べる。

### 自動判断は片方だけ足さない
「大差がついたら控えを出す」を野手にだけ入れていたので、
**打線は控えなのにエースが投げ続けている**という試合になっていた。
同じ場面判断（`isGarbageTime`）を両方から呼ぶこと。

### 積み上げる値を表示の桁で丸めない
評判を `Math.round` で整数にして保存していたので、
**1試合ぶんの加算（評判40で0.47）が毎回切り捨てられていた**。
負けは素の値が大きく（-0.86）丸めを生き延びるため、
「勝っても動かないのに負けると下がる」状態になり、大会に優勝しても数字が動かなかった。
小数で保ち、**表示のときだけ丸める**こと。

### 鈍らせ方は「釣り合う比」で確かめる
評判の加算を `room²` で鈍らせていたら、**評判50で加算が4分の1、64で8分の1**しか通らず、
勝率6割のチームが30前後で頭打ちになっていた。
鈍らせる係数を入れたら、**上がる側と下がる側が釣り合う比**を計算して確かめること
（50で2:1、64で4.9:1 が必要だった）。

### 難易度を「進捗」で決め打ちしない
大会の相手を「1回戦は格下・決勝は格上」という曲線で決めていたので、
**どの大会も同じ筋書き**になり、抽選の妙が消えていた。
トーナメントなら、運が悪ければ1回戦から優勝候補と当たるのが当たり前。

**開幕時に山を引き、難易度は勝ち残りやすさから自然に出す**（`drawTournament`）。
1回戦は全校が同じ重みで、回戦が進むほど強い学校の重みが増える。
先に相手が決まるので、同じ学校が二度出てくる事故も構造的に起きない。

### 「達成したときだけ」の報酬は途中経過を消す
大会の評判を「優勝したときだけ」出していたので、
**決勝で負けた年と初戦で負けた年が同じ扱い**だった。
勝ち上がりに意味を持たせるなら、到達段階に応じて配ること。

### 上限を作ったら下限も作る
評判は「上に行くほど上がりにくい」だけを入れていた。
そこへ**負けたら下がる**を足したら、勝てないチームの評判が一桁まで落ちて、
新入生も部費も枯れて立て直せなくなった（実測で60年後に7）。
片側だけ鈍らせると、逆側に張り付く。
`reputationGainAt` は上下どちらも鈍らせている。

### 別の物差しに移すときは、その物差しの定数も全部直す
プロ入りで能力を半分にしたとき、戦力外の閾値（45）や出場率の式を
高校基準のまま残していたら、**入団した年に全員が戦力外**になるところだった。
スケールを変えたら、そのスケールを読む定数を洗い出して同時に直すこと
（`career.ts` の `RELEASE_THRESHOLD` / `OVERSEAS_THRESHOLD` / `playRate`）。

### 相対評価の仕組みを足すとき
U18代表のように「他校と比べて」で決まる仕組みは、**比較対象を state に持つ**。
その場で作った相手と比べても、翌年には別人になっていて基準が動かない。
`rivals`（ライバル校）が基準を持ち続ける役割を担っている。

さらに、**「いちばん強い1人」と比べてはいけない。**
その1人が卒業した年だけ基準が急に下がる。
枠が18人あるなら**18番目の選手**を基準にする（`upperStarRatingAtRank`）。
比較の母集団も、県内ではなく**全国**にする。県内にすると
「参加校の多い県を選ぶと代表に選ばれにくい」という筋の通らない差がつく。

### 「他校」を足すとき
選手データを他校ぶんまで丸ごと持つとセーブが膨らむ。
持つのは「学校の戦力」と「注目選手（2人）」と**名簿の種（`rosterSeed`）**だけ。

**使い捨てで作るのはやめた。** 同じ学校と2回戦っても別人が出てくるし、
スカウトで逃した選手がその学校に居ることもなく、
「あの学校のあの選手」という手応えが生まれなかった。

いまは種から**毎回同じ名簿を作り直す**（`rivalRoster`）。保存量は数値ひとつのまま。
**入学年ごとに種を分ける**のが要点で、こうすると今年の1年生が
来年も同じ名前の2年生として出てくる。

決まった種から作るときは、**代をまたいだ状態を持ち込まない**こと。
名前の重複避けを3学年ぶんまとめて行っていたら、
「その年に居合わせた他学年」によって名前が変わり、
今年の1年生が来年は別人になっていた。


戦力は**必ず地力へ引き戻す**（`REGRESSION`）。引き戻さないと
強豪が際限なく強くなるか、弱小が消えていく。

### `Math.round` は -0 を返す
`Math.round(-0.2)` は **-0**。JSON にすると `0` になるので、
セーブして読み直すと値が変わり「JSONに変換できる」テストが落ちる（実際に落ちた）。
負になりうる値を丸めるときは 0 に潰すこと。

### 「外す」操作に自動補充を足さない
ベンチ入りから外す仕組みで、定員を自動補充していたため
**落とした選手がいちばん強い控えとしてその場で戻ってきて**、入れ替えができなかった。
プレイヤーの操作（`trimSquad`）は減ったままにし、
補充するのは部員が入れ替わったとき（`repairSquad`）だけにする。

### 画面下部の操作を押し出さない
可変長のパネル（成長結果など）は**必ず高さの上限を置く**。
上限が無いと下にある手札やボタンを画面外へ押し出す（実際に押し出された）。
`max-height` ＋ `overflow-y: auto` で中身をスクロールさせる。

### ドラッグ操作を作るとき
**HTML5 の drag&drop（draggable / dragstart）は使わない。** タッチ端末で動かない。
`pointerdown` / `pointermove` / `pointerup` で自前に組み、
掴んでいる要素には `touch-action: none` を付ける（付けないとスクロールに取られる）。
落とし先は `document.elementFromPoint()` と `data-*` 属性で拾う。

**`touch-action: none` は「つまみ」だけに付ける。** 行全体に付けると、
その行の上では**ページがスクロールできなくなる**（スタメン画面が見出し付近でしか
スクロールできず、実際に操作不能になった）。行の左端に専用のつまみ（`⠿`）を置き、
そこにだけ `touch-action: none` を置く。行の他の場所は指を滑らせればスクロールする。

### 見た目でチームと個人を区別する
帽子とユニフォームは**チームで共通**にする（`teamColors(schoolName)`）。
同じ部の選手なのに一人ひとり色が違うのは不自然。
選手ごとの区別は**ポジションの系統で色分けしたネームプレート**で付ける。

### カード全体をタップできるようにするとき
カード内にボタンを置くなら、**カード自体を `<button>` にしない**。
`<button>` の入れ子は不正なHTMLで、React が実行時エラーを出す（実際に出た）。
型チェックでは捕まらないので、**必ずブラウザのコンソールまで確認する**。

### アセットを増やさない
画像と同じ理由で、**音声ファイルも持たない**。効果音は `src/ui/sound/sound.ts` で
Web Audio API から合成する。外部通信なしで鳴り、オフラインでも動く。

- `AudioContext` は**最初のユーザー操作まで作らない**（ブラウザの自動再生制限）。
- 効果音のON/OFFは端末の設定なので localStorage に保存し、**セーブデータには入れない**。
- 描画ライブラリ（Pixi.js など）は入れない。SVGで足りている。

### PWA / Service Worker
- **アイコンだけは PNG を持つ。** 画像を持たない方針の唯一の例外で、
  **iOS は `apple-touch-icon` の SVG を無視する**（追加してもアイコンが出ない）。
  PNG は `scripts/build-icons.mjs` で生成する（依存は増やさず zlib で書く）。
  **SVG を直したら生成スクリプトの図形も直すこと。**
- **`install` で `skipWaiting()` しない。** 画面に古い JS が載ったまま
  新しい SW が配信を始めると、取りに行くアセットと噛み合わなくて壊れる。
  待機させて `UpdateBanner` で知らせ、プレイヤーが選んだときだけ差し替える。
- インストールできることは**案内しないと気づかれない**。
  iOS には `beforeinstallprompt` が無いので、手順を文章で出すしかない。
- `public/sw.js` は**ビルドのファイル名ハッシュに依存させない**。
  事前にファイル一覧を焼き込むと、ビルドのたびに壊れる。
- 開発中は登録しない（`import.meta.env.PROD` で分岐）。HMRと衝突する。
- キャッシュ名 `CACHE` を変えると古いキャッシュが破棄される。
  配信物の構成を大きく変えたときは上げること。

### 勝手に部費を使わない
練習試合マスに止まった瞬間、相手も遠征先も遠征費も勝手に決まっていた。
プレイヤーは**引かれたあとで気づく**しかなく、断ることもできなかった。
費用が発生する行動は、**候補を出す → 選ぶ → そこで初めて引く**の順にする。
選択肢には「やらない」を必ず混ぜること。

### 画面を奪う処理の前には、直前の結果を見せる間を作る
練習の成長を出すより先に試合の画面へ切り替わっていたので、
**カードを選んだ結果を一度も見ないまま試合が始まっていた。**
`growthReport` のように**フェーズだけを先送りする**足止めを挟む。
効果そのものは先に解決しておくこと。閉じるまで乱数の続きが決まらないと、
中断してセーブしたときに再開できなくなる。

### 共通の判定関数は「呼び手の立場」に依存させない
`isGarbageTime(team, ctx)` が相手を `ctx.defenseTeam` で決め打ちしていたので、
**守備側を渡した投手交代の側だけ点差が常に0**になっていた。
攻撃側を渡す野手交代は正しく動いていたため、
「打線は控えなのにエースが投げ続ける」が再発していた（2度目）。
両方から呼ぶ関数は、**渡された `team` から見た相手を毎回引き直す**こと。

### 効果は定義に数値で書き、判定はそれを読む
特殊能力の補正を `simulateAtBat` に直書きしていたので、
**説明だけあって試合では何も起きない特殊能力**が3つ混ざっていた
（守備範囲拡大・レーザービーム・クイック）。
定義に `effects` として数値を持たせ、判定も表示もそこを読む形にすれば、
「書いてあるのに効かない」も「効くのに書いていない」も起きない。
新しい効果先を足すときは、`SKILL_TARGET_LABELS` と `SKILL_TARGET_UNIT`
（表示用の対応表）も同時に埋めること。テストで穴を検出している。

### 選ぶ側と判定する側で同じ物差しを使う
`simulateAtBat` は球威（球速7割）で打たれ方を決めているのに、
先発と継投を決める `pitcherValue` に球速が1ミリも入っていなかった。
147km/hのエースが制球のいい142km/hの投手にマウンドを譲り、
野手能力も高いばかりに別のポジションやベンチへ回っていた。
**「試合で強い」と「自動編成が選ぶ」が食い違ってはいけない。**
式は `stuffScore` のように1か所に置いて共有する。

### 「特定のマスでしか起きない」は判断を奪う
能力の成長を**練習マスに止まったときだけ**にしていたので、
カードで何を選ぶかより「練習マスを踏めるか」で育成が決まっていた。
数字は移動距離の意味しか持たず、緑マスに止まると1ミリも伸びない。
**主要な効果はプレイヤーが毎回選ぶもの（カード）に載せ、
マスはそれを増減させる補助にする。**

### greedy で埋めたら、あとから入れ替えて詰める
守備の重要度順にスタメンを埋めたので、後回しの一塁・左翼に**余り物しか来なかった**
（「順番に取ると、先に取った枠が得をする」の再発。今度は枠の順序ではなく
「枠ごとに見る物差しが違う」ことが原因）。
枠ごとに評価軸が違うなら、**埋め終わってから2人ずつ入れ替えて総和を上げる**
（`improveBySwaps`）。枠が9つなら1巡28通りで、数巡あれば落ち着く。

### 大きなブロックを一括置換するときは端を確かめる
`describe('冬合宿')` から `describe('日付固定イベント')` までを差し替えたつもりが、
**その間にあった8つの describe（約680行）ごと消していた**。
テストは通ってしまうので気づかない（消えたテストは走らない）。
開始と終了の位置を指定して置換するときは、**置換後に `git diff --stat` の
削除行数を必ず見る**こと。想定より桁が違えば巻き込んでいる。

### 「全部ある」ことでしか作れないものがある
大会の相手を10校の中から引いていたので、**トーナメント表が作れなかった**。
「どこが勝ち上がったか」を見せるには、参加校がそのまま存在していないといけない。
持ち物を減らす工夫（種から名簿を作り直す）は、
**存在そのものを減らす言い訳にはならない**。
178校あっても、名前と数値だけならセーブは30KBしか増えない。

### 参加者を増やしたら「全員ぶん出す」を疑う
県内を10校から178校にしたら、
「はっきり戦力が動いた学校」の報告が毎年数十件出るようになり、
世代交代の画面が他校の話で埋まった。一覧も縦に伸びて操作不能になった。
**数が増える変更をしたら、その数を舐めている表示を全部探す**こと
（`newsFor` の `notable` 判定、`RivalsTab` の `LOCAL_PREVIEW`）。

### 決まった種から作るとき、前の要素の乱数消費を持ち込まない
`rivalRoster` は代ごとに1本の乱数で5人を作っていた。
投手の持ち球の数は能力で変わるので**引く回数が変わる**。
学年が上がると能力の基準も上がるため、
同じ代なのに翌年は3人目以降が別人になっていた。
**要素ごとに種を分ける**（`playerSeed(rosterSeed, enrolledYear, index)`）。

### 固定の一覧から「重複しない値」を引き続けない
大会の相手を `OPPONENT_NAMES`（数十件）から重複なしで引くループを書いたら、
一覧を使い切った時点で**無限ループ**になった（テストが返ってこなくなった）。
組み合わせで作る `makeSchoolName` は、
リトライ上限を超えたら重複を許して**必ず返す**。

### 「◯手で届くはず」とテストに書かない
補充された手札の数字は固定ではないので、
`for (let i = 0; i < 8; i++)` のような手数の決め打ちは
編成を触るたびに落ちる。**条件で回す**（`while (next.month < 6)`）。

### 図の中に文字を置くと、値によって重なる
持ち球の矢印の先に球種名を書いていたので、
左下・真下・右下のように**向きが近い球種**を持った投手だけ、
「スローカーブ」「フォーク」の字が重なって読めなくなっていた。
図形の位置がデータで決まるなら、**文字は図の外に出す**。
向きは矢印（↙↓↘）などの記号で対応づける。

### 狭い枠に入るかは幅を数えて決める
1行に詰め込んだ文字列は、375pxでは必ずどこかで折り返す。
「OB名鑑（プロ入り 9人） ▶」は「9／人）▶」で切れ、
OB名鑑の「13年目卒 / P / 高校時80 → プロ44 / 横浜マリンスターズ」は
球団名の途中で切れていた。
**折り返してほしくない塊は要素に分けて `white-space: nowrap` を付ける**。
分けた要素は行を変えるか、`text-overflow: ellipsis` で切る。
親に `display:flex` を付けるときは `min-width: 0` を忘れない
（付けないと省略記号が効かない）。

### 同じ部品を狭い枠にも置くなら列数を渡す
投手の能力を2列で組んだら、スタメン画面の右の枠（100px）で
「スタミナ」の行が枠外へ出た。
**枠の幅は置き場所ごとに違う**ので、部品側に `columns` や `narrow` を持たせて
呼び出し側が指定する。CSS Modules は外から上書きしにくい。

### 上限は「収入が意味を失う値」になっていないか確かめる
部費の上限を999,999にしていたので、評判が上がって月5万入るようになると
数年で張り付き、**それ以降は入ってきた部費が消えるだけ**になっていた。
上限を置くなら、想定プレイ年数ぶんの収入を足して届かないことを確かめる。

### 既製の記号で段階を表せるか、段階の数で確かめる
弾道4段階を矢印文字（→↗↑）で表そうとしたら、使える向きが45度刻みしか無く、
弾道2と3が同じ字面になるか、**弾道1が「下向き（↘）」という
打球としてありえない意味**になっていた。
段階の数と記号の数が合わないなら、SVGで描く。

### 図形の座標をデータで決めるなら、両端で枠に収まるか確かめる
弾道の矢印を24×32の枠に描いたら、一番立った角度（65度）だけ
矢じりが上へはみ出して、1つ上の行に食い込んでいた。
**一番寝た値と一番立った値の両方**を入れて枠を決めること。

### 選択対象が変わる場所の state は key でリセットする
スタメン画面の右の枠に「投手能力／野手能力」の切り替えを足したら、
投手を見たあとに野手を選んでも「投手能力」を選んだ状態が残った。
React は同じ位置の要素を使い回すので、
**選択対象が変わったら作り直す**（`<AbilityPanel key={player.id} …>`）。

### 表示のランクと判定の尺度は、対応表で縛る
球速だけ km/h の実数値なので、ランクの色が付けられなかった。
別に「表示用のランク関数」を作ると、
**画面はDなのに判定では85相当**という食い違いがまた生まれる。
`velocityScore` の対応表（`VELOCITY_ANCHORS`）を
ランクの境界（`RANK_THRESHOLDS`）に合わせて置き、
`toRank(velocityScore(v))` をそのままランクとして使う。
テストで全 km/h について一致を確認している。

### 届かない最高ランクは、無いのと同じ
球速の上限を158にしたまま「160以上がS」というランク表を入れたら、
**Sに構造上どうやっても届かなかった**。
ランクの境界を決めたら、その値に到達しうるか上限を確かめる。

### 尺度を組み直したら、その尺度を読む定数を全部測り直す
球速の尺度をランクに合わせ直したら、同じ投手のスコアが20下がり、
- 打率が .263 → .275（投手が弱くなった）
- 投手の総合だけ野手より10低くなった（`pitchingRating` の比重）
- 生成時の球速が素質と釣り合わなくなった（`velocityFor` の切片）
- 肩力が一律に下がった（`armFromVelocity`）
の4か所が同時に狂った。
**尺度を触ったら、それを引数に取る式を grep して全部見る**こと。

### 「重みを上げる」場所は、総合か試合かを区別する
「球速の補正を大きく」と言われて総合の比重も上げると、
**総合が低く出る能力の比重を上げる**ことになって逆効果になる。
総合はベンチ入りやドラフトで**役割をまたいで比べる値**なので偏らせない。
重みを上げるのは試合の判定（`stuff` と三振率）のほう。

### 上限に張り付くと、能力表が「全部同じ色」になる
評判が上がったチームは、どの能力もA・Sまで届いていた。
うちの4番はミートもパワーも走力も守備もS——という並びになり、
**誰が何の選手なのか読めなくなる**。
頭打ち（`diminishingMultiplier`）は「Bまでは順調、そこから先は選ぶ」
くらいに絞ると、能力表が選手の個性として読めるようになる。

### 既定の動作が「全部等倍」だと、みんな同じ形に育つ
おまかせ練習ですべての能力を等倍で伸ばしていたので、
遊撃手のパワーも一塁手の走力も同じだけ伸び、
3年経つと誰を見ても同じレーダーになっていた。
既定にも**傾き**を持たせる（`POSITION_GROWTH`）。
ただし**強制にはしない**（幅0.7〜1.35）。
選手ごとの得意・苦手のほうを大きく取っておけば、
「遊撃手だが打てる」も普通に生まれる。
傾きを足すときは**平均が1.0になるよう配る**こと。
そうしないと全体の成長量まで動いてしまう。

### 「稀に飛び抜ける」を混ぜないと、入学時点で決着する
球速はドラフトの分かれ目（150km/h）なのに、伸び方が全員同じだった。
これだと**入学時の球速だけで3年後が決まり、育てる余地が無い**。
20人に1人、その能力だけ飛び抜けて伸びる素材を混ぜる
（`LATE_BLOOMER_CHANCE`）。

### 「一部だけ振る」と、残りが揃って個性が消える
伸び代を「得意2つ・苦手2つ・残りは等倍」で配っていたので、
**半分の能力がちょうど1.0で並んでいた**。
3年育てると、どの選手も同じような形のレーダーに落ち着く。
個性を出したいなら、**全項目に別々の値**を振る。
そのうえで「選手そのものの伸びやすさ」を別の軸に持たせないと、
項目ごとの乱数は平均に寄って「よく伸びる選手」が作れない
（`TALENT_SPREAD` と同じ話）。

### ばらつきを大きくしたいなら、合計を0に揃える
能力ごとの凸凹を素直に `rng.int(-spread, spread)` で振ると、
たまたま全部プラス／全部マイナスに寄って**総合そのものがずれる**。
スカウトの「素質◯◯」と入学後の総合が食い違う原因になる。
平均を引いて中心に戻せば（`centeredSpread`）、
総合を動かさずに凸凹だけを大きくできる。

### 効果を測るテストは、その効果が乗る指標で測る
「広角打法（パワー+8）で長打が増える」を長打全体で測っていたが、
二塁打はパワーに依存しない（`doubleShare` は固定）ので、
**二塁打のノイズに効果が埋もれて**編成を触るたびに落ちていた。
本塁打だけで測れば素直に出る。
指標を選ぶ前に、その値がどの能力で動くのかを式で確かめること。

### idで引き当てるなら、id の形式がぶつからないことを確かめる
他校の部員の id（`${'${校id}'}-${'${入学年}'}-${'${背番号}'}`）と
注目選手の id がまったく同じ形式で、
**同じ名簿の中に同じ id の選手が2人現れる**ことがあった。
U18の名簿は id で選手を引き当てるので、
「総合99で選んだはずが、引き当てたら総合71の別人」という取り違えが起きていた。
テストで名簿内の id が一意であることを縛ってある。

### 「作り直す」ものは、作り直せなくなる日が来る
他校の部員は種から作り直しているので保存が要らない——のはずが、
3年生は年度が替わると名簿から消え、
注目選手由来の選手は卒業と同時に種からも再現できなくなる。
**参照先が消える可能性があるものは、参照だけで持ってはいけない。**
在籍中は作り直し、消えたときのために姿だけ写す、の両立にした。

### 数値をそのまま別の意味に流用しない
学校の戦力（`strength`）をそのまま選手の素質に足していたので、
戦力46の学校の3年生が素質96になり、能力が上限に張り付いていた。
U18の名簿を作ったら「30人全員が総合98〜99」という並びになり、
自校の選手は何をどう育てても届かなかった。
学校の戦力は**層の厚さ**であって、突出した個人の多さではない。

### 上限のある値を計算で作ったら、その場で丸める
`velocityFor` が `VELOCITY_MAX` で止めていなかったので、
素質の高い他校の投手が169km/hで生成され、
球速スコアが飽和して総合100の投手が量産されていた。
生成側と表示側の両方で clamp すること。

### 「1つしか無い」ものは、選ぶ意味も出会う驚きも生まない
県外を1県1校にしていたので、遠征で同じ県へ行くたびに
**必ず同じ学校が相手**になった。その県には1校しか存在しないので当然で、
「遠征先で新しい相手と当たる」という手応えが出ない。
数を増やすときは、**1つの塊の中にも序列を付ける**こと
（全部を「全国クラス」にすると、今度はどこへ行っても格上になる）。

### 母集団を増やしたら、そこから何を選ぶのかを見直す
県外を1県1校から4校にしたら、全国大会（各県1代表）の抽選に
**同じ県から何校も出てくる**ようになった。
母集団を増やす変更は、その母集団を読んでいる箇所を全部見ること
（`nationalRepresentatives` を足して、代表だけを甲子園に出す形にした）。

### 世代交代する要素には、必ず入学年を持たせる
開始時の注目選手だけ入学年を持っていなかったので、
`starGrade` が学年を据え置き、**3年生が何年経っても3年生のまま**
他校の名簿に居座っていた。U18の名簿で「卒業しない選手」として現れて発覚した。
あとから追加した生成経路（`advanceRival`）では渡していたのに、
最初の生成（`createRivals`）で渡し忘れていた形。

### 「年々増えるもの」と「数が多いもの」を分けて考える
他校を946校まで増やせたのは、**他校が卒業生を持たない**から。
部員は種から作り直すので、学校が抱えるのは名前と数値と注目選手2人だけで、
60年進めても他校ぶんは +23KB しか増えない（増えるのは対戦成績だけ）。
セーブが膨らむかどうかは**校数ではなく、1件あたりが年々太るかどうか**で決まる。
増やす前に、その要素が年々太る構造になっていないか確かめること。

### 既定値を全件に持たせない
対戦成績（`record`）を全校ぶん空で持たせていたので、
946校で17KBがまるごと無駄になっていた。
「まだ何も起きていない」は**持たない**で表し、
読むときにヘルパー（`recordOf`）で既定値を返す。

### 一意にする範囲は、必要な範囲だけにする
校名の重複を全国で避けていたので、946校ぶんの名前を
1426通りの組み合わせから引くことになり、後半で引き当てられなくなっていた。
違う県に同じ校名があるのは現実にもあることで、避ける必要が無い。

### キー名が中身より重くなったら、保存だけ配列にする
他校2818校の JSON は1校115文字で、うち半分以上がキー名と
`regionId` の文字列だった（実際の値は40文字）。
配列に詰め替えるとキー名が丸ごと消え、**324KB → 112KB** になる。

**`GameState` の型は変えないこと。** 詰め替えるのは `src/save` の中だけで、
読み込んだ時点で元の形に戻す。core が保存の都合を知ってはいけない（1.1）。
古いセーブ（詰められていない形）もそのまま読めるようにしておく。

### 「無い」を 0 で表すと、0 が有効な値のときに壊れる
詰めた配列の末尾から「無い」ものを切り落とすとき、
`0` も落としていたので**入学年 -1・戦力 0 の学校が壊れた**
（ゲーム開始年の3年生は入学年が -1 になる）。
「無い」は `null` で表し、切り落とす条件も `null` だけにする。

### 「出発点」は、目指す先から逆算して置く
初期部員を素質どおり（補正0）で作っていたので、
**県内のちょうど真ん中の水準**（平均総合43・最高71）で始まっていた。
「弱小校から強豪へ」というゲームなのに、最初から中位校だった。
出発点を決めるときは、他と比べて**どの位置にいるべきか**を測ってから置く
（`INITIAL_TALENT` は他校の戦力 -18 相当に合わせてある）。

### 出発点を下げたら、到達点も一緒に確かめる
初期部員と新入生を弱くしたら、10年後の最高総合が 77 → 68 まで落ちた。
弱くしたいのは**序盤**で、育て切った先ではない。
評判の効き（`REPUTATION_TALENT_RATE`）を上げて到達点を戻し、
上限（`RECRUIT_MAX_TALENT`）で「新入生が前の代の3年生を超える」のを防いだ。

### 診断の前提が変わったら、診断も直す
`balanceCheck` の試合は「初期部員 vs 戦力0の相手」で打率を測っていた。
初期部員を弱くしたら一方的な試合になり、
**打撃と投球の釣り合いを測るという診断の意味が消えていた**
（打率.243・勝率34%）。相手の戦力も同じだけ下げて対称に戻した。

### 相対値（±）で出された数字は、比べる相手が無いと読めない
他校の強さを「戦力 +15」と出していたが、
**自分のチームと比べてどうなのかが分からなかった**。
選手の総合と同じ物差し（`B(70)`）で出せば、そのまま比べられる。
`ratingLabel()` に一本化してある。

### 一覧に出す数字は、開いたときの中身と一致させる
学校の戦力から「スタメン平均の見込み」を立てて一覧に出したら、
タップして開いた9人の実際の平均と5ほどずれていた。
**実測できるなら実測する。** 重いなら、
出す行だけ計算して並べ替えもその値で行う（`pickRivals`）。
選ぶ段階だけ軽い指標（戦力）を使えば、2818校ぶんを走らせずに済む。

### 同じものを作る経路が2つあるなら、必ず突き合わせる
試合の相手は、実在の学校なら `rivalRoster`（戦力の55%）、
学校が分からなければ `createDisposableOpponent`（戦力そのまま）で作っていた。
**甲子園の代表校より「名も無い相手」のほうが強い**という状態になっていて、
`opponentRating`（格付けと評判）も強い学校を過大評価していた。

### 色の名前を文字として書かない
マスに「緑」と書いてあるのに水色、「青」と書いてあるのに緑、
という食い違いが起きていた。**色は色で分け、文字は意味を書く**
（練／良／悪／？／休／効）。
色名を書くと、色を調整するたびに文字も直さなければならなくなる。

### 同じ色の系統が2つ以上あるなら、頻度の高いほうを沈める
練習マス（最頻出）と良いマスが両方とも緑で見分けられなかった。
色数には限りがあるので、**よく出るほうを落ち着いた色にして**、
たまに出るほうに明るい色を残す。

### 形で見せるか、数字で見せるかは「何を知りたいか」で決める
野手の能力を六角形のレーダーで出していたが、
形は「打撃型か守備型か」を伝えるだけで、
**ミートがA(83)なのかB(75)なのかは読めない**。
一覧で選手を比べる場面で知りたいのは数字のほうだった。

### 「候補を集める関数」に、居場所の思い込みを入れない
継投の候補を「ベンチにいる投手」だけで集めていたので、
**野手として出ている投手には一度も出番が回らなかった**。
ベンチに投手が残っていなければ、エースが何点取られても投げ続ける。
`availableRelievers` の `!onField.has(p.id)` のような条件は、
「交代とは控えを出すこと」という思い込みがそのまま書かれた形になっている。
守備からマウンドへ回すのも交代で、そのときは**誰も退かない**。

### 入れ替えたら、周りも組み直す
野手として出ている投手をマウンドへ上げるとき、
その2人の守備位置を入れ替えるだけだと、
**球威で選ばれた投手がそのまま遊撃を守る**並びになる。
1か所動かしたら、影響する範囲をまとめて組み直す
（`reassignFieldPositions`。打順は試合中に動かせないので触らない）。

### 枠に入らないのは、たいてい中身ではなく字の大きさの問題
留学生の名前を「カルロス メンドーサ」で作ったら、
選手カードのネームプレートで**姓も名も切れて「カル…／メンド…」**になった。
そこで名簿側を4文字までに縛ったが、今度は
**留学生の名前がどれも短く、同じような響きに寄ってしまった**。

中身（名前）を削る前に、**その長さのときだけ字を小さくする**ことを考える
（`nameFontSize`。3文字までは今までどおりの大きさで、4文字から縮み始める）。

ただし**縮められる下限はある**。9pxでも12文字は入らないので、
姓と名を合わせた長さのほうを縛る（`EXCHANGE_NAME_TOTAL_MAX`）。
1つあたりを縛ると長い名前が丸ごと消えるが、
合計で縛れば「アレハンドロ ラミレス」は残せる。

**縮める判断は、合計と最長の両方で決める。**
合計だけで見ていると、同じ10文字でも 3+7 のような偏った名前で片方が省略記号になる。

### 何も起きないカードを手札に残さない
治療カードは、怪我人がいなければ
「幸い、怪我をしている部員はいなかった」で終わるだけのカードだった。
**選ぶ意味が無いのに手札の枠を1つ塞ぐ**ので、手を進めるためだけに切ることになる。
壊れた器具の練習を引き直すのと同じ扱いにする
（`isUseless` で抽選から外し、`replaceUselessCards` で手札からも引き直す）。

### 「年度の中で動かない」ものは、卒業しても下がらない
他校の名簿は「学年のベース＋学校の戦力」で作っていたので、
3年生が抜けても翌年の3年生が同じ基準で作られ、
**卒業で1ミリも下がらなかった**。こちらだけが弱くなる世界になる。
年度の中で伸びる項（`RIVAL_YEAR_GROWTH`）を入れて初めて、
卒業の谷とそこからの持ち直しが生まれる。

**足す項は年度の真ん中を0にする。** 4月を0にして積み上げると、
1年を通した平均が丸ごと上がってしまう
（実測で甲子園出場が30年7.6回→4.0回に落ちた）。
動かしたいのが「年度の中での上下」なら、年間の平均は動かしてはいけない。

### 少人数の平均は、格付けを逆転させる
他校の部員を自校と同じ振れ幅（±18）で作っていたので、
1学年5人しかいない名簿では平均が±8揺れ、
**地力64の名門が地力37の学校より弱く見える**ことが普通に起きていた。
「名門は強い」という一覧の読み方が成り立たないなら、
その一覧は嘘をついている。母数が小さいところでは振れ幅を絞る。

### 一覧の並べ替えに使う値は、比較のたびに計算しない
`rosterPowerOf`（乱数を3回引く）で8000校をソートしたら**374ms**かかった。
比較関数は O(n log n) 回呼ばれるので、1件ぶんの計算が重いとそのまま効いてくる。
**先に1回だけ計算して添える**（decorate-sort）。
そもそも乱数カーソルを作らずハッシュで出せば、それだけで42msになる。

### 「全部見る」は、母数が増えた瞬間に壊れる
他校の一覧に「残り◯校を見る」を置いていた。2640校のときも重かったが、
7900校になると**実測で1.6秒＋7900行**で画面が固まる。
母数を増やす変更をしたら、**全件を舐めるUIを必ず探す**
（上限を決めて「さらに20校」に変えた）。

### 自分だけに掛かる頭打ちは、世界を歪める
自校の選手は練習の頭打ち（`diminishingMultiplier`）で85を超えたあたりから
ほとんど伸びないのに、他校の選手は**生成されるだけ**なのでその上限を無視していた。
結果、U18代表の30人が全員90超え・6人が95以上という並びになり、
自校の選手はどう育てても届かない。
**育てて届く上限と、生成で出る上限は、同じ物差しに揃える**
（`rosterTalentOf` で上ほど鈍らせる）。

### 上限を下げると、その値を読む式も一緒に狂う
`rosterTalentOf` で戦力→素質の効きを鈍らせたら、
戦力から相手の強さを見込む式（`opponentRating` ＝ 比例）だけが古いままで、
**強豪を過大評価する**状態になった。
「別の物差しに移すときは、その物差しの定数も全部直す」と同じ話で、
変換を1か所に足したら、**その変換を経ずに同じ値を読んでいる場所**を必ず探す。

### 「伸びにくい」と「伸びない」は違う
高い能力ほど伸びにくくする係数を、90以上で0.03まで絞っていた。
3年かけても1つ上がるかどうかで、**事実上そこで凍りついていた**。
頭打ちは「そこから先は伸ばす能力を選ぶ」ための仕組みであって、
上限を作るためのものではない。緩めるときは**上の帯だけ**にする
（中ほどまで緩めるとチーム全体が一段強くなり、難易度が丸ごと動く）。

### 段階の意味は、数字で見せれば説明が要らない
守備適性を S〜G の8段階で出していたが、
**「G」が何割の力なのかは画面から読めなかった**（実際は0.3倍）。
5段階にして「1段＝守備力の20%」と決め、
画面には段数ではなく**その位置で出せる守備力の数字**を出すようにしたら、
説明も凡例も要らなくなった。判定に使う値（`defenseScore`）をそのまま出すので、
表示と判定が食い違いようもない。

### 段階を作り直したら、生成・変換・保存・表示の4か所を必ず揃える
適性を5段階に変えたとき、直すのは型だけではない。
生成（`createAptitudes`）、コンバートの上限（`CONVERT_MAX`）、
移行（`migrate` v38→v39）、表示（ダイヤ図・スタメン・練習画面）が全部繋がっている。
**移行を忘れると既存のセーブが壊れる**ので、型を変えたらまず migrate を書く。

### 現実の分布から外れた値は、上限で気づけない
球速を素質に比例させていたら、他校の3年生が軒並み160km/h超えになり、
**U18代表の投手が全員160〜165km/h**という並びになった。
`VELOCITY_MAX`（165）で止まっていたので数字としては壊れておらず、
一覧を見るまで気づけない。
実在の分布（高校生で150km/h台後半は全国に数人）が分かっているものは、
**その分布に合わせて効きを鈍らせる**。

### 格は「能力」ではなく「戦績」で表せる
強豪校を地力（隠し値）で決めていたので、
**学校を強くする＝そこに居る個人を強くする**になり、
U18代表が総合95〜100で埋まった。
「勝っている学校が強豪」にすれば、能力を1ミリも上げずに格を表せる。
毎年どこかが県を勝ち、そのうちの1校が全国を獲る、という記録を積むだけでいい
（`runRivalSeason`。48県ぶんの試合は回さず、力に応じた重み付き抽選で決める）。

### 学年のベースを引き忘れると、上の学年ほど強くなる
注目選手を `talentBonus = rating - GRADE_BASE[1]` で作っていたので、
3年生は `GRADE_BASE[3] + (rating - GRADE_BASE[1])` ＝ **素質+14** で生成され、
「素質93」と書いてあった選手が全能力100になっていた。
**素質を指定して作るときは、その学年のベースを引く。**

### 「学校を強くする」と「個人を強くする」は同じ蛇口
名門のスタメン平均をA(84)まで上げると、その学校に居る個人も95〜100になり、
U18代表が90台で埋まる。**チームの格と個人の上限は同じ値から出ている**ので、
片方だけ上げることはできない。
上げられるのは層の厚さ（部員数）だけで、それも母数が増えるぶん
上振れが出やすくなる。学校の格は「中位校との差」で表すのが安い。

### 年に数回しかない結果を、ログに流さない
合宿の成果（誰が何の特殊能力を掴んだか）をログのメッセージにしていた。
年2回しかない出来事なのに、他の報告に混ざって流れていくので**読まれない**。
足を止めて一覧で見せる（`CampReport`）。
**同じ意味のものは同じ形で見せる**ほうがいいので、
その日の成長（`GrowthReport`）の並びとCSSをそのまま借りている。

### 同じ言葉で2つの数字を出さない
能力表の「守備 C69」と、守備適性の図に出る本職の数字（64）が食い違っていた。
守備適性のほうは守備・捕球・肩を混ぜた値だったからで、
式としては筋が通っていても、**同じ「守備」という言葉で2つの数字が出たら
どちらかが嘘**になる。混ぜたい理由があるなら、名前を変えるか、混ぜるのをやめる。

### 加わったものは、加わった場所すべてに出す
スカウトで獲った選手は名簿には加わるのに、
世代交代の「新入生」の一覧には出てこなかった（別の配列で持っていたため）。
**同じ意味のもの（今年入部した選手）は、1つの並びにまとめてから配る。**

### 「能力が動かない選択肢」は、動いたものを出さないと無かったことになる
ミーティングとメンタル強化は信頼度とやる気を上げるのに、
成長の報告が**能力の変化しか出していなかった**ので、
一覧が空のまま「目に見える変化は無かった」と表示されていた。
効いているのに何も起きていないように見えるなら、それは効いていないのと同じ。
**その選択肢が動かすものを、その場で出す**（`PendingGrowth.notes`）。

### 「写真を生成する」はこのゲームでは成立しない
顔写真を都度生成するには、モデル（数十〜数百MB）を積むか外部サービスに投げるかで、
**オフラインで動く・画像アセットを持たない・セーブは localStorage**
という前提のどれかが必ず崩れる。
仮に生成できても、1枚50〜200KB×30人×数十年ぶんは localStorage（5MB）に入らない。
「写真のように見せたい」なら、**陰影で立体を出したベクター**が現実的な上限。

### 骨格を乱数で振ると、人の顔に見えなくなる
顔の生成で、目の間隔・顎の幅・顔の長さまで振っていたら、
組み合わせによって**人の顔に見えないもの**が混ざった。
似顔絵は「配置は同じで、パーツが違う」で十分に描き分けられる。
振るのは**パーツの形と色**だけにして、位置は定数で固定する。

### 顔のような「絵」は、縦位置を数字で置く
輪郭・帽子・肩をそれぞれ比率（顔の長さの0.7倍…）で組んでいたので、
乱数で顔が縦長になった選手だけ**あごが肩に埋まる**、
横広の選手だけ**帽子が浮く**という壊れ方をした。
目・あご・つばの高さを定数で置き、他のパーツはそこから測る。

### 効果が全員同じだと、誰が来ても同じ
マネージャーの効果を役割ごとの固定値にしていたので、
入部してきたのが誰であっても結果が変わらなかった。
**「良いのが来た」という手応え**が出ないなら、個人差を持たせる
（`managerPower`）。ただし下限は0にしない（居るのに何も起きないのでは意味が無い）。

### 選考は「対象が居るうち」に行う
U18代表の選考を11月にしていたので、**夏で引退した3年生は絶対に選ばれなかった**。
代表の主力は3年生なのに、自校からは2年生以下しか候補に挙がらない。
名簿から消える処理（引退・卒業）があるなら、それを使う処理は**消える前**に走らせる。

### 4段階しかない値は、他の能力と同じ刻みで扱わない
弾道は1〜4しかないので、練習で1ずつ上がると全員がすぐ4になる。
**他の能力に換算した練習量を溜めて、届いたところで1段上げる**
（`TRAJECTORY_COST` ＝ パワー12ぶん。実測で97手＝3か月半）。
持ち球（`PITCH_COST`）も同じ形にしてある。

### 質を表す値は、掛かる相手に掛け算で乗せる
ノビ（ストレートの威力）を球威に**足して**いたら、
球の遅い投手がノビだけで速球投手に並んでいた。
「◯◯の質」を表す値は、**その◯◯に掛ける**（`qualityFactor`。50で等倍・±15%）。
中央を等倍にしておけば、値を足しても投手全体が強く（弱く）ならない。

### 意味の重なる能力を2つ置かない
「変化球（どれだけ曲がるか）」と「キレ（その曲がりが効くか）」を別々に持たせたが、
**画面に並ぶと見分けが付かず、どちらを伸ばせばいいのかも分からなかった**。
掛け算で合成していたので、片方だけ高くても意味が無く、実質1つの能力だった。
能力を足すときは「その2つが**別々に動く理由**が説明できるか」を先に確かめる。
統合するときは**どちらか一方を捨てない**（主だったほうを重く見て混ぜる）。

### 選手の写しは1か所ではない
`Player` の型を変えたとき、`players` と `graduates` だけ移行して
**世代交代の画面で待っている新入生（`pendingSeason.newcomers`）**を忘れた。
ノビ・キレの欄が空のまま並ぶ新入生ができた（ブラウザで実際に出た）。
`matchState`・`pendingSeason`・スカウトの結果など、写しは state のあちこちにある。
数え上げると必ず取りこぼすので、**保存データ全体を歩いて形で見つける**
（`fixPitchingDeep`）。

### 保存に入れ忘れた値は、無かったことになる
他校の戦績（`titles`）を `packRivals` に入れ忘れていたので、
**保存して読み直すと甲子園の出場回数が丸ごと消えていた**。
何年プレイしても名門が生まれないのに、
「戦績が付くのは翌年から」に見えるので気づけない。
`GameState` に項目を足したら、**詰め替えにも足したか**を必ず確認する。

### 「その役割では使わない能力」しか伸びないカードは、死んだ選択肢
打撃・走塁・遠投のカードで投手が伸ばせるのは打力だけだったので、
**手札の半分が投手には意味の無いカード**だった
（実測で投手の総合は1年+2.75、野手は+4.75）。
役割ごとに効かないカードがあるなら、**その役割にとっての意味に読み替える**
（`PITCHER_REDIRECT`。振る力は投げる力、走れる下半身は投げ続ける下半身）。
読み替えを足しても、**元の能力も少しは伸ばしたまま**にすること。

### 避けられない停止で、選んだカードの価値を削らない
大会・合宿のマスは飛び越えられないので、5のカードでも1マスしか進まないことがある。
成長も体力の回復も進んだ日数に比例させていたため、
**大会の直前に休養カードを切ると、ほとんど回復しないまま試合に入っていた**。
プレイヤーには避けようがなく、画面のどこにも理由が出ない。
移動だけを止めて、練習・休養そのものはカードどおりに入れる（`effectSteps`）。

### 顔の部品は、動く基準に合わせて付ける
耳を固定の高さ（目の5下）に置いていたので、
頬の位置が高い輪郭では**あごの細くなった所に耳がぶら下がって**見えていた。
輪郭は10種類あって頬の高さが -7〜0 で動くのに、耳だけ動かなかったのが原因。
輪郭を返す関数は、**そこに付く部品の基準（`cheekY`）も一緒に返す**。

### 物差しは増やしてよいが、尺度は1つに保つ
球速のランクを物理の尺度（160km/hでS）で付けていたので、
**高校生の球速はどれだけ鍛えてもD〜Bの帯に固まっていた**。
かといって尺度そのものを動かすと、試合・総合・肩力・成長の頭打ちが全部ずれる。

**尺度（`velocityScore`）は1つのまま、ランクを付ける物差しを増やす**
（`velocityGrade` ＝高校生、`velocityScore` ＝プロ）。
「同じ値でも、誰と比べるかで意味が変わる」ものは、比べる相手のほうを持ち替える。
表示の物差しを足したら、**ゲージも同じ物差しにする**
（Aなのにゲージは半分、という見え方になる）。

### 生成の帯を動かしたら、その帯を読む定数を全部測り直す
球速の生成を7km/h下げたら、打率が .254 → .277 に跳ねた（投手が一律に弱くなった）。
同時に肩力・投手の総合・ドラフトの球速ボーナスもずれていた。
`STUFF_BASELINE`・`VELOCITY_STRIKEOUT_RATE`・`ARM_BASE`・`DRAFT_VELOCITY_LINE` を
測り直して元の水準に戻すまでが1つの変更。

### 平均で測ると、一芸が消える
選手の値打ちを能力の加重平均（総合）で出していたので、
**オールCの選手が、ミートとパワーだけSで他がGの選手より上**に並んでいた。
その物差しでU18代表を選ぶので、代表は毎年**万能型ばかり**で顔ぶれが変わらない。
平均は「穴が無いか」を測るものであって、「どれだけ戦力になるか」ではない。

**高い能力ほど1点の値打ちを上げて合計する**（`playerPoints`。S×2.5〜G×1）。
足す前に係数を掛けるだけで、突き抜けた一芸が平均に薄められなくなる。

物差しを増やしたら、**どれを何に使うかを決めて書く**こと
（評価点＝選手の値打ち／総合＝他校の戦力とドラフト）。混ざると意味が分からなくなる。

### 役割ごとに届く上限が違うと、点数順で片方が全滅する
評価点で並べてU18代表30人を切ったら、**投手が0人**になった。
野手は6項目すべてがSになりうるのに対し、
投手は球速（高校生で150km/hはS＝ほぼ出ない）も持ち球もその帯まで届きにくい。
点数の物差しをひとつにするなら、**役割ごとの枠を先に取る**
（`U18_MIN_PITCHERS` / `MIN_SQUAD_PITCHERS`）。

### 練習でしか育たない値は、生成される選手ではゼロのまま
持ち球の変化量は練習で上がる値なので、生成時は1〜2で固定していた。
自校の投手は育つが、**他校の投手は全員が「曲がらない球を2つ」**のまま。
評価点が変化量を重く見るようになった途端、他校の投手が構造的に低く出た。
生成されるだけの選手にも、**育った先の姿を持たせる**
（`initialPitchLevel`。キレ70で変化量4）。

### 出発点の「らしさ」は、数字ではなく現物で確かめる
初期部員を「弱小校の水準」で作っているつもりが、
**140km/h を投げる3年生が40人に1人**いた。平均で見ている限り気づけない。
その設定の学校に居るはずのない選手が混ざっていないかは、
分布の端（最速・最高）を出して確かめる（`INITIAL_TALENT` -10 → -18 で
140km/h以上は410人中1人になった）。

### 評価の仕組みを変えたら、生成の振れ幅も見直す
能力の凸凹（`ABILITY_SPREAD`）を狭くしていたのは、
平均で評価していた頃は**凸凹が大きいほど損**だったから。
評価点が一芸を正当に評価するようになったなら、振れ幅を広げてよい
（±18 → ±24。凸凹の平均は25→31になった）。
ただし守備の穴も増えるので、打率が上がる（実測 .254 → .262）。

### 物差しを変えたら、色を決める関数も変える
評価点（0〜1200）を表示に切り替えたとき、色だけ `toRank`（0〜100用）のままにしていた。
**どの選手も90以上なので、全員がSの色**で並ぶ。
数字そのものは正しいので、パッと見では気づけない（ユーザーの画面写真で見つかった）。
値の桁を変えたら、その値を読む**表示側の関数も全部**探すこと
（`pointsRank` / `rankColorOf`）。

### 呼び名と中身は同時に作る
「名門」と表示していた学校のスタメン平均が65（C）で、中堅と見分けが付かなかった。
呼び名（戦績）だけ先に作って、中身（戦力）を上げていなかったのが原因。
しかも閾値が緩く、地力28の学校まで名門と呼ばれていた。
格を出すなら、**その格らしい中身になっているかを分布で確かめる**
（いまは名門が県に1校・スタメン平均72.8）。

### チームの強さを平均で出さない
学校の戦力を「スタメン9人の平均総合」で出していたので、
**飛び抜けた選手を抱えた学校が、平らな学校に埋もれていた**。
平均は穴の無さを測る値で、戦力そのものではない。
チームの強さは**評価点の合計**（`lineupPointsOf` / `teamPoints`）で出す。
選手ひとりの評価点と地続きの数字になり、
「この学校はうちのエース何人ぶんか」がそのまま読める。

物差しを揃えるときは、**その値が出ている画面を全部**直すこと
（ホーム・データ・他校一覧・トーナメント表・試合前）。
1か所でも平均のままだと、同じ「強さ」で桁の違う数字が並ぶ。

### 伸びの土台を下げるときは、代わりの伸ばし方を用意する
「能力が上がりやすすぎる」を `CARD_GROWTH_SCALE` を下げるだけで直すと、
**どの選択も等しく遅くなるだけ**で判断は増えない。
下げたぶんは**プレイヤーが動かせるもの**に回す
（グラウンドの効きを 1.6倍 → 2.1倍。整備するかどうかの差が2倍以上になる）。

### 上がった評判は、目に見えるものに変える
評判は新入生の質にしか効いていなかったので、
強豪と呼ばれるようになっても**練習環境は土のグラウンドのまま**だった。
積み上げた数字は、プレイヤーが毎日見る場所に反映されないと実感にならない
（`groundFloorFor`。評判80でLv20まで学校が整えてくれる）。
ただし**自分で上げる余地は必ず残す**（支援だけで最大まで行けると整備が死ぬ）。

### 同じ効果のカードを何枚も置かない
信頼度が上がるだけのカードが3枚あった（ミーティング・メンタル強化・自主学習）。
どれを引いても同じことが起きるので、**手札に3種類あっても選択肢は1つ**だった。
効果が重なったら、片方を別の役割に振り直す
（信頼度＝ミーティング／捕球＝メンタル強化／学力＝自主学習）。

役割を移すときは**練習系の総量が増えていないか**も見ること
（メンタル強化を練習カードにしたら、6年後の到達点が45→48に戻ってしまった。
重みを元に戻して45.5に収めた）。

### 避けられない行事には、避ける手段を持たせる
定期テストは全員の体力を削るだけの行事で、年2回必ず来るマイナスでしかなかった。
学力（`academics`）で結果が変わるようにすると、
**自主学習カードを切るかどうか**という判断が生まれる。
「避けられないマイナスは軽く、選択の結果として受けるマイナスは重く」の応用で、
**先に手を打てば避けられる**形にするとさらに良い。

### 負荷は「絶対量」ではなく「その人の余力に対する割合」で数える
投手の疲労をアウト数に比例させていたので、
**スタミナSの投手が7回投げるほうが、Dの投手が5回投げるより疲れていた**。
余力のある選手ほど同じ仕事で疲れるのは逆で、
負荷は**自分の持ち（`staminaCapacity`）に対する割合**で数える。

### 「できなくなる」と「下手になる」は別のペナルティ
疲労を「スタミナの目減り」で表していたので、
疲れた投手も1人目の打者には本調子と同じで、ただ早く降板するだけだった。
「疲れている」の実感は**最初から球威も制球も落ちている**ほうで、
イニングを食えるかどうかは別の話。
どちらの形にするかは「プレイヤーに何を我慢させたいか」で決める
（今回は「出せるが打たれる」を選んだ）。

### 権利について
本家タイトルのアセット・キャラクターデザイン・ロゴを模倣しない。
参考にしてよいのは「画面構成」（情景を常時表示する、カードを絵で見せる等）までとする。

## 4. テスト

- `src/core` に書いたロジックは **ユニットテストを必ず添える**（`*.test.ts` を同じ階層に置く）。
- UIコンポーネントのテストは MVP 段階では不要。
- テスト対象の優先度：成長計算 > マス解決 > カード抽選 > 打席シミュレーション。
- 乱数を使う関数は、固定シードを渡して結果を検証する。

```bash
npm run test        # ← CI と同じコマンドで確かめる
npm run typecheck   # コミット前に必ず通す
```

**確認は `npm run test` で行うこと。** `--testTimeout` を足して走らせていたら、
CI（既定の5秒）でだけ落ちた。他校が2818校あるので `startedGame` は重く、
数年ぶんを回す診断は既定のタイムアウトを超える。
**診断テストには `}, DIAG_TIMEOUT)` のように明示的な上限を書く。**

---

## 5. 作業の進め方

### 実装を始める前に
1. 変更対象がどのレイヤー（core / state / ui / save）か明示する。
2. 新しい型が必要なら **先に `src/core/types/` を更新** してから実装に入る。
3. 3ファイル以上に触れる変更は、先に手順を提示して確認を取る。

### 実装するとき
- **一度に1機能だけ。** 依頼されていないリファクタリングや機能追加をしない。
- 既存のコードスタイル・命名に合わせる。
- 実装後は `npm run typecheck` と `npm run test` を実行し、結果を報告する。
- 動かないもの・未検証のものを「完成しました」と報告しない。

### やらないこと
- 依頼なしでライブラリを追加しない（追加が必要なら理由と代替案を提示して確認する）。
- 依頼なしで `README.md` / `CLAUDE.md` を書き換えない。
- 実在の球団名・選手名・大会名・楽曲・ロゴを使わない（**権利侵害の回避**）。
- 大量のファイルを一度に生成しない。

---

## 5.5 試合を触るときの注意

**シミュレーション（結果を決める）と再生（表示する）を絶対に混ぜない。**

さらに、**試合はマスに止まった時点ではシミュレートしない。**
`lineupCheck` フェーズでスタメンを確認させ、`startMatch` で初めて
試合を組み立てる。先に走らせると、確認画面で編成を変えても結果が変わらない。

**試合は半回ずつ進む。** `stepHalfInning()` を呼んだ回数だけ進み、
回の切れ目で `MatchState` が state に残る。ここに選手交代を挟む。

- `MatchState` は **JSON にそのまま変換できる形しか持たない**（Map / Set 禁止）。
  中断してセーブされるため。`Player.history` は試合に使わないので落とす。
- 交代を挟まなければ乱数の並びは変わらないので、
  **どの速度で見ても結果は同じ**という保証はそのまま生きている。
- テストで試合を最後まで進めるときは `autoPlay.ts` の `runMatch()` を使う。
  `startMatch` だけでは1球も投げていない状態で止まる。

```
startMatchState() → MatchState
      ↓ stepHalfInning() を半回ずつ          ← core、純粋関数
finalizeMatch()   → MatchResult（全打席の記録）
                      ↓
        UI が speed に応じて再生するだけ      ← ui、結果に影響しない
```

- `src/core/match/` に演出・タイミング・setTimeout を持ち込まない。
- 観戦速度を変えても結果が変わってはいけない。**速度は表示だけの設定**。
- 試合結果のチームへの反映（信頼度・体力）は `finishMatch` でのみ行う。
  観戦中に反映すると、途中離脱で二重適用される。
- 試合中は他画面へ移動させない（`App.tsx` で `phase === 'match'` を最優先で分岐）。

## 5.55 盤面は1マス1日・1年365マス

月ごとのゴールは無い。4月1日から3月31日まで1本の道が続く。

- **1マス＝1日**（`DAYS_PER_CELL`）。1年は `CELLS_IN_YEAR = 365` マス。
  日とマスの換算は `src/core/calendar/days.ts` の `dayOfCell()` / `cellOfDay()` だけを使う。
  いまは恒等写像だが、**マス番号と日を直接同一視するコードを書かない**
  （3日刻みにしていた時期があり、また変えるかもしれない）。
- **能力が伸びる土台はカードにある。** 数字＝進む日数＝練習した日数で、
  止まったマスに関係なく毎手必ず伸びる（`CARD_GROWTH_SCALE`）。
  体力の消耗も同じく日数に比例する（`CARD_COST_SCALE`）。
  止まったマスは倍率で効く補助に回っている（`CELL_GROWTH_BONUS`）。
  カード定義の `amount` は「しっかり練習した1日でこれくらい」という読みやすい値のまま残す。
- **マスの重みも組み直す。** 手数が3倍になると、練習試合も特訓も怪我も年3倍起きる。
  `CELL_WEIGHTS` は「1年に何回あってほしいか」から逆算し、増えたぶんは
  練習マスと空きマスに寄せてある。
- 月をまたぐ処理（部費・維持費・体力回復・急成長・固定イベント・能力の記録）は
  `applyOneMonth()` にまとめ、`selectCard` が **またいだ月ぶん繰り返し呼ぶ**。
  カード1枚で2ヶ月ぶんまたぐことがあるので、1回だけ呼ぶ実装にしてはいけない。
- **4月（年度初め）も同じ月次処理を通す。** 通さないと4月の行事（入学式）だけ起きない。
  `advanceYear` の中で明示的に呼んでいる。
- 大会・合宿のマスは `forcedStopBetween()` で**飛び越えを禁止**する。
- **大会は1回戦ずつ別のマスに置く。** `placeTournamentCells()` が現在地の先へ
  `ROUND_GAP` マス間隔で回戦ぶんのマスを並べる。1つ勝つたびに `cardSelect` へ戻り、
  次の回戦のマスまでは普通に練習できる。負けた／優勝したら
  `clearTournamentCells()` で残りのマスを消す。**消さないと同じ大会が再開する。**
- **カードの数字は「進む日数（1〜5）」であり、同時に「練習した日数」でもある。**
  1年の日数は365で固定なので成長の総量は打ち方によらず一定だが、
  スケール（`CARD_GROWTH_SCALE`）を変えると成長速度が直接動く。
  必ず `seasonBalance.test.ts` を回して確認する。

### 小さい値を四捨五入しない
1手あたりの体力消耗が2〜3という小さな値になったとき、`Math.round` で丸めたら
**性格やマネージャーの2割の差が消えた**（「やんちゃは消耗が激しい」が成立しなくなった）。
1未満の差を持つ値は、成長計算と同じ**確率で切り上げる**丸めにする。
そのぶんテストも1回では判定できないので、平均を取って比べること。

## 5.6 新しいフェーズを足すときの注意

`GameState.phase` に値を足したら、次の3つを必ずセットで用意する。

1. `App.tsx` … そのフェーズ専用の画面へ強制的に飛ばす（他画面へ行かせない）
2. **`src/core/autoPlay.ts` の `playUntilMonthEnd()` に分岐を足す**
3. そのフェーズから抜けるコマンド（`finishMatch` / `finishSeason` / `chooseCampPlan` など）

`match` / `newSeason` / `tournament` / `camp` を足したとき、**4回とも**進行ループが止まった。
当初は診断・テストそれぞれに同じ分岐を書いていたため毎回あちこちが壊れたので、
**進行ループは `autoPlay.ts` に一本化した**。ここだけ直せば全部追従する。

さらに `tournament` では、月を進める前に開催判定をしていたため、
大会終了後に同じ月で無限に再開催されるループも起きた。
**フェーズから抜ける経路が「元の判定に戻らない」ことを必ず確認する。**

フェーズを足したら、単体テストだけでなく**必ず診断スクリプトを走らせる**：
```bash
npx vitest run src/core/seasonBalance.test.ts --disable-console-intercept
```

## 6. セーブデータの扱い

- `GameState` は **そのまま JSON にシリアライズできる形** に保つ（Map / Set / class / 関数を入れない）。
- `GameState` に `version: number` を必ず持たせる。
- 型を破壊的に変更したら `src/save/migrate.ts` に移行処理を追加する。
- 保存キーは **1枠目が `hs-baseball-sim:save:v1` 固定**（キーは変えず、中の `version` で管理する）。
  2枠目以降は `:2` `:3` を足す。**1枠目のキーを変えてはいけない**（既存のセーブが読めなくなる）。
- 現在の `SAVE_VERSION` は 40。v1→v40 の移行が `migrate.ts` にある。
**選手の型を変えたら `matchState` の中の写しも直す。** 試合の途中でセーブしていると
両チームの選手が `matchState` に入っていて、ここを忘れると
再開した試合で球威が NaN になる（v39→v40 がその例）。
  **盤面の刻み（`DAYS_PER_CELL`）を変えたら移行が必須。** `boardPosition` の意味が
  変わるので、`cellOfDay()` で読み替えて盤面を作り直す（v22→v23 がその例）。
- `Player.history`（能力の推移）のように**毎月増える配列**を足すときは、必ず上限を決めて
  `slice(-LIMIT)` で切る。セーブが際限なく膨らんで localStorage の容量を超える。

---

## 7. コミットメッセージ

```
feat: 練習カードの抽選ロジックを追加
fix: 能力値が上限100を超えるバグを修正
refactor: マス解決処理を resolveCell に切り出し
docs: 成長曲線のバランスメモを追記
test: growth.ts のユニットテストを追加
chore: Vitest を導入
```
- 本文は日本語。1コミット1目的。

---

## 8. 応答スタイル

- 回答・説明・コード内コメントは**日本語**。
- 変更内容は「どのファイルを・なぜ変えたか」を簡潔に述べる。長い前置きは不要。
- 仕様が曖昧な場合は、勝手に決めずに確認する。ただし細かな実装判断（変数名など）は自分で決めてよい。
